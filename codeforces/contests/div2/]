#include<bits/stdc++.h>

using namespace std;
int main(){
    unsigned long long int t, s ,a, b, c;
    cin >> t;

    while(t--){
        cin >> s >> a >> b >> c;
        unsigned long long int x = trunc(s/(a*c));
        cout << x*a + x*b << endl;
        
    }

    return 0;
}
